local Settings = {
    ESP     = { Enabled = true, Skeleton = true, Box = true, Line = true, Name = true, Health = true, Thickness = 0.6, LinePos = "TOP" },
    Sdamage = { Enabled = false, Amount = 100 },
    CurrentTab = "ESP"
}

local RunService       = game:GetService("RunService")
local Players          = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer      = Players.LocalPlayer
local Camera           = game.Workspace.CurrentCamera

local AutoColors = {
    Color3.fromRGB(255, 255, 255),
    Color3.fromRGB(0,   120, 255),
    Color3.fromRGB(255, 220, 0),
    Color3.fromRGB(255, 40,  40),
    Color3.fromRGB(180, 0,   255),
    Color3.fromRGB(0,   255, 220),
}
local autoColorIndex   = 1
local autoColorTimer   = 0
local currentAutoColor = AutoColors[1]

RunService.RenderStepped:Connect(function(dt)
    autoColorTimer = autoColorTimer + dt
    if autoColorTimer >= 1.2 then
        autoColorTimer   = 0
        autoColorIndex   = (autoColorIndex % #AutoColors) + 1
        currentAutoColor = AutoColors[autoColorIndex]
    end
end)

task.spawn(function()
    while true do
        task.wait(0.05)
        if Settings.Sdamage.Enabled then
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character then
                    local hum  = player.Character:FindFirstChild("Humanoid")
                    local root = player.Character:FindFirstChild("HumanoidRootPart")
                    if hum and root and hum.Health > 0 then
                        local _, onScreen = Camera:WorldToViewportPoint(root.Position)
                        if onScreen then
                            hum:TakeDamage(Settings.Sdamage.Amount)
                        end
                    end
                end
            end
        end
    end
end)

local espConnections = {}

local function CreateFullESP(player)
    if espConnections[player] then
        espConnections[player]:Disconnect()
        espConnections[player] = nil
    end

    local esp = {
        box       = Drawing.new("Square"),
        line      = Drawing.new("Line"),
        healthBar = Drawing.new("Line"),
        name      = Drawing.new("Text"),
    }
    local limbs = {}
    for i = 1, 14 do limbs[i] = Drawing.new("Line") end
    esp.box.Filled          = false
    esp.name.Size           = 12
    esp.name.Center         = true
    esp.name.Outline        = true
    esp.healthBar.Thickness = 1.5

    local function drawBone(line, p1, p2)
        if p1 and p2 and p1 ~= p2 and Settings.ESP.Enabled and Settings.ESP.Skeleton then
            local a = Camera:WorldToViewportPoint(p1.Position)
            local b = Camera:WorldToViewportPoint(p2.Position)
            if a.Z > 0 and b.Z > 0 then
                line.From      = Vector2.new(a.X, a.Y)
                line.To        = Vector2.new(b.X, b.Y)
                line.Color     = currentAutoColor
                line.Thickness = 0.5
                line.Visible   = true
                return
            end
        end
        line.Visible = false
    end

    local function HideAll()
        for _, v in pairs(esp) do pcall(function() v.Visible = false end) end
        for i = 1, #limbs do pcall(function() limbs[i].Visible = false end) end
    end

    local function Update()
        local ok, err = pcall(function()
            local char = player.Character
            local hum  = char and char:FindFirstChild("Humanoid")
            if not char or not char:FindFirstChild("HumanoidRootPart") or not hum then
                HideAll()
                return
            end

            local root          = char.HumanoidRootPart
            local pos, onScreen = Camera:WorldToViewportPoint(root.Position)
            local distance      = (Camera.CFrame.Position - root.Position).Magnitude

            if onScreen and Settings.ESP.Enabled then
                local sizeX = math.clamp(1000 / distance, 10, 150)
                local sizeY = sizeX * 1.6

                if Settings.ESP.Box then
                    esp.box.Visible   = true
                    esp.box.Size      = Vector2.new(sizeX, sizeY)
                    esp.box.Position  = Vector2.new(pos.X - sizeX/2, pos.Y - sizeY/2)
                    esp.box.Color     = currentAutoColor
                    esp.box.Thickness = 0.5
                else esp.box.Visible = false end

                if Settings.ESP.Line then
                    esp.line.Visible = true
                    if Settings.ESP.LinePos == "CENTER" then
                        esp.line.From = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)
                    elseif Settings.ESP.LinePos == "BOTTOM" then
                        esp.line.From = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y)
                    else
                        esp.line.From = Vector2.new(Camera.ViewportSize.X/2, 0)
                    end
                    esp.line.To           = Vector2.new(pos.X, pos.Y - sizeY/2)
                    esp.line.Color        = currentAutoColor
                    esp.line.Thickness    = 0.5
                    esp.line.Transparency = math.clamp(1 - (distance/1200), 0.35, 0.95)
                else esp.line.Visible = false end

                if Settings.ESP.Health then
                    local hp = math.clamp(hum.Health / math.max(hum.MaxHealth, 1), 0, 1)
                    esp.healthBar.Visible = true
                    esp.healthBar.From  = Vector2.new(pos.X - sizeX/2 - 5, pos.Y + sizeY/2)
                    esp.healthBar.To    = Vector2.new(pos.X - sizeX/2 - 5, pos.Y + sizeY/2 - sizeY * hp)
                    esp.healthBar.Color = Color3.fromRGB(math.floor(255 - 255*hp), math.floor(255*hp), 0)
                else esp.healthBar.Visible = false end

                if Settings.ESP.Name then
                    esp.name.Visible  = true
                    esp.name.Text     = player.Name .. " [" .. math.floor(distance) .. "m]"
                    esp.name.Position = Vector2.new(pos.X, pos.Y - sizeY/2 - 16)
                    esp.name.Color    = currentAutoColor
                else esp.name.Visible = false end
            else
                esp.box.Visible       = false
                esp.line.Visible      = false
                esp.healthBar.Visible = false
                esp.name.Visible      = false
            end

            local isR15 = char:FindFirstChild("UpperTorso") ~= nil
            local bones = {}
            if isR15 then
                local head   = char:FindFirstChild("Head")
                local uTorso = char:FindFirstChild("UpperTorso")
                local lTorso = char:FindFirstChild("LowerTorso")
                local lUA    = char:FindFirstChild("LeftUpperArm")
                local lLA    = char:FindFirstChild("LeftLowerArm")
                local lHand  = char:FindFirstChild("LeftHand")
                local rUA    = char:FindFirstChild("RightUpperArm")
                local rLA    = char:FindFirstChild("RightLowerArm")
                local rHand  = char:FindFirstChild("RightHand")
                local lUL    = char:FindFirstChild("LeftUpperLeg")
                local lLL    = char:FindFirstChild("LeftLowerLeg")
                local lFoot  = char:FindFirstChild("LeftFoot")
                local rUL    = char:FindFirstChild("RightUpperLeg")
                local rLL    = char:FindFirstChild("RightLowerLeg")
                local rFoot  = char:FindFirstChild("RightFoot")
                bones = {
                    {head,   uTorso}, {uTorso, lTorso},
                    {uTorso, lUA},    {lUA,    lLA},    {lLA, lHand},
                    {uTorso, rUA},    {rUA,    rLA},    {rLA, rHand},
                    {lTorso, lUL},    {lUL,    lLL},    {lLL, lFoot},
                    {lTorso, rUL},    {rUL,    rLL},    {rLL, rFoot},
                }
            else
                local head  = char:FindFirstChild("Head")
                local torso = char:FindFirstChild("Torso")
                local lArm  = char:FindFirstChild("Left Arm")
                local rArm  = char:FindFirstChild("Right Arm")
                local lLeg  = char:FindFirstChild("Left Leg")
                local rLeg  = char:FindFirstChild("Right Leg")
                bones = {
                    {head,  torso},
                    {torso, lArm}, {torso, rArm},
                    {torso, lLeg}, {torso, rLeg},
                }
            end
            for i = 1, #limbs do
                drawBone(limbs[i], bones[i] and bones[i][1], bones[i] and bones[i][2])
            end
        end)
        if not ok then HideAll() end
    end

    espConnections[player] = RunService.RenderStepped:Connect(Update)

    player.CharacterAdded:Connect(function()
        task.wait(0.5)
        CreateFullESP(player)
    end)
end

for _, p in pairs(Players:GetPlayers()) do
    if p ~= LocalPlayer then CreateFullESP(p) end
end
Players.PlayerAdded:Connect(function(p)
    if p ~= LocalPlayer then CreateFullESP(p) end
end)
Players.PlayerRemoving:Connect(function(p)
    if espConnections[p] then
        espConnections[p]:Disconnect()
        espConnections[p] = nil
    end
end)

local function valToStr(v)
    local t = typeof(v)
    if t == "string"  then return '"' .. v:sub(1, 40) .. '"' end
    if t == "number"  then return tostring(math.floor(v * 100) / 100) end
    if t == "boolean" then return tostring(v) end
    if t == "nil"     then return "nil" end
    if t == "Instance" then return v:GetFullName() end
    if t == "Vector3"  then return "V3(" .. math.floor(v.X) .. "," .. math.floor(v.Y) .. "," .. math.floor(v.Z) .. ")" end
    if t == "CFrame"   then return "CFrame" end
    return tostring(v)
end

local SpyAddEntry

local oldNamecall
oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
    local method = getnamecallmethod()
    if method == "FireServer" or method == "InvokeServer" then
        local args = {...}
        local argParts = {}
        for _, v in ipairs(args) do
            table.insert(argParts, valToStr(v))
        end
        local argStr   = table.concat(argParts, ", ")
        local fullPath = pcall(function() return self:GetFullName() end) and self:GetFullName() or "?"
        local copyText = fullPath .. ":" .. method .. "(" .. argStr .. ")"
        if SpyAddEntry then
            SpyAddEntry(method, fullPath, argStr, copyText)
        end
    end
    return oldNamecall(self, ...)
end)

local ScreenGui = Instance.new("ScreenGui", LocalPlayer:WaitForChild("PlayerGui"))
ScreenGui.Name         = "呵呵"
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size                   = UDim2.new(0, 500, 0, 310)
MainFrame.Position               = UDim2.new(0.5, -250, 0.5, -155)
MainFrame.BackgroundColor3       = Color3.fromRGB(6, 7, 12)
MainFrame.BackgroundTransparency = 0.0
MainFrame.Visible                = false
MainFrame.Active                 = true
MainFrame.Draggable              = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 14)

local BgImage = Instance.new("ImageLabel", MainFrame)
BgImage.Size                 = UDim2.new(1, 0, 1, 0)
BgImage.BackgroundTransparency = 1
BgImage.ImageTransparency    = 0.6
BgImage.ScaleType            = Enum.ScaleType.Crop
BgImage.ZIndex               = 0
BgImage.Image                = "https://img1.pic.in.th/images/078fd4be39028c992.jpg"
Instance.new("UICorner", BgImage).CornerRadius = UDim.new(0, 14)

local Overlay = Instance.new("Frame", MainFrame)
Overlay.Size                   = UDim2.new(1, 0, 1, 0)
Overlay.BackgroundColor3       = Color3.fromRGB(5, 6, 12)
Overlay.BackgroundTransparency = 0.45
Overlay.ZIndex                 = 1
Instance.new("UICorner", Overlay).CornerRadius = UDim.new(0, 14)

local MainStroke = Instance.new("UIStroke", MainFrame)
MainStroke.Thickness       = 1.5
MainStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

local Header = Instance.new("Frame", MainFrame)
Header.Size                   = UDim2.new(1, 0, 0, 48)
Header.BackgroundColor3       = Color3.fromRGB(6, 7, 14)
Header.BackgroundTransparency = 0.05
Header.ZIndex                 = 3
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 14)

local LogoFrame = Instance.new("Frame", Header)
LogoFrame.Size                   = UDim2.new(0, 36, 0, 36)
LogoFrame.Position               = UDim2.new(0, 6, 0.5, -18)
LogoFrame.BackgroundColor3       = Color3.fromRGB(0, 90, 190)
LogoFrame.BackgroundTransparency = 0.4
LogoFrame.ZIndex                 = 4
Instance.new("UICorner", LogoFrame).CornerRadius = UDim.new(0, 8)

local LogoImg = Instance.new("ImageLabel", LogoFrame)
LogoImg.Size                  = UDim2.new(1, 0, 1, 0)
LogoImg.BackgroundTransparency = 1
LogoImg.ZIndex                = 5
LogoImg.Image                 = "https://img1.pic.in.th/images/1-22.jpg"
Instance.new("UICorner", LogoImg).CornerRadius = UDim.new(0, 8)

local LogoFallback = Instance.new("TextLabel", LogoFrame)
LogoFallback.Size                  = UDim2.new(1, 0, 1, 0)
LogoFallback.BackgroundTransparency = 1
LogoFallback.Text                  = "呵呵"
LogoFallback.Font                  = Enum.Font.GothamBold
LogoFallback.TextSize              = 10
LogoFallback.TextColor3            = Color3.fromRGB(0, 220, 255)
LogoFallback.ZIndex                = 4

local Title = Instance.new("TextLabel", Header)
Title.Text             = "呵呵"
Title.Size             = UDim2.new(0, 200, 0, 48)
Title.Position         = UDim2.new(0, 50, 0, 0)
Title.TextColor3       = Color3.fromRGB(255, 255, 255)
Title.Font             = Enum.Font.GothamBold
Title.TextSize         = 15
Title.BackgroundTransparency = 1
Title.TextXAlignment   = Enum.TextXAlignment.Left
Title.TextYAlignment   = Enum.TextYAlignment.Center
Title.ZIndex           = 4

local Version = Instance.new("TextLabel", Header)
Version.Text             = "v3.0"
Version.Size             = UDim2.new(0, 60, 0, 48)
Version.Position         = UDim2.new(0, 116, 0, 0)
Version.TextColor3       = Color3.fromRGB(80, 100, 160)
Version.Font             = Enum.Font.Gotham
Version.TextSize         = 10
Version.BackgroundTransparency = 1
Version.TextXAlignment   = Enum.TextXAlignment.Left
Version.TextYAlignment   = Enum.TextYAlignment.Center
Version.ZIndex           = 4

local CloseBtn = Instance.new("TextButton", Header)
CloseBtn.Text                = "X"
CloseBtn.Size                = UDim2.new(0, 40, 1, 0)
CloseBtn.Position            = UDim2.new(1, -44, 0, 0)
CloseBtn.Font                = Enum.Font.GothamBold
CloseBtn.TextColor3          = Color3.fromRGB(255, 80, 80)
CloseBtn.TextSize            = 14
CloseBtn.BackgroundTransparency = 1
CloseBtn.ZIndex              = 5

local ToggleBtn = Instance.new("TextButton", ScreenGui)
ToggleBtn.Size                   = UDim2.new(0, 56, 0, 56)
ToggleBtn.Position               = UDim2.new(0.9, 0, 0.15, 0)
ToggleBtn.Text                   = "呵呵"
ToggleBtn.Font                   = Enum.Font.GothamBold
ToggleBtn.TextSize               = 11
ToggleBtn.TextColor3             = Color3.fromRGB(0, 220, 255)
ToggleBtn.BackgroundColor3       = Color3.fromRGB(8, 9, 16)
ToggleBtn.BackgroundTransparency = 0.15
ToggleBtn.Active                 = true
ToggleBtn.Draggable              = true
Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(0, 50)
local ToggleStroke = Instance.new("UIStroke", ToggleBtn)
ToggleStroke.Thickness = 1.5

ToggleBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)
CloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
end)

local Sidebar = Instance.new("Frame", MainFrame)
Sidebar.Size                   = UDim2.new(0, 128, 1, -56)
Sidebar.Position               = UDim2.new(0, 4, 0, 52)
Sidebar.BackgroundColor3       = Color3.fromRGB(6, 7, 14)
Sidebar.BackgroundTransparency = 0.05
Sidebar.ZIndex                 = 3
Instance.new("UICorner", Sidebar).CornerRadius = UDim.new(0, 10)
local SidebarList = Instance.new("UIListLayout", Sidebar)
SidebarList.Padding             = UDim.new(0, 4)
SidebarList.HorizontalAlignment = Enum.HorizontalAlignment.Center
SidebarList.VerticalAlignment   = Enum.VerticalAlignment.Top
local SidebarPad = Instance.new("UIPadding", Sidebar)
SidebarPad.PaddingTop = UDim.new(0, 8)

local Container = Instance.new("Frame", MainFrame)
Container.Size                = UDim2.new(1, -146, 1, -58)
Container.Position            = UDim2.new(0, 140, 0, 54)
Container.BackgroundTransparency = 1
Container.ClipsDescendants    = true
Container.ZIndex              = 3

local Pages = {}
local function CreatePage(tabName)
    local page = Instance.new("ScrollingFrame", Container)
    page.Size                 = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.CanvasSize           = UDim2.new(0, 0, 3, 0)
    page.ScrollBarThickness   = 2
    page.ScrollBarImageColor3 = Color3.fromRGB(0, 180, 255)
    page.Visible              = false
    page.ZIndex               = 3
    local layout = Instance.new("UIListLayout", page)
    layout.Padding = UDim.new(0, 5)
    Pages[tabName] = page
    return page, layout
end

local EspPage,     EspLayout     = CreatePage("ESP")
local SdamagePage, SdamageLayout = CreatePage("SDAMAGE")
local SpyPage,     SpyLayout     = CreatePage("SPY")

SpyPage.CanvasSize = UDim2.new(0, 0, 0, 0)
SpyLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    SpyPage.CanvasSize = UDim2.new(0, 0, 0, SpyLayout.AbsoluteContentSize.Y + 10)
end)

local TabButtons = {}
local function SwitchTab(tabName)
    Settings.CurrentTab = tabName
    for name, page in pairs(Pages) do
        page.Visible = (name == tabName)
    end
    for name, btn in pairs(TabButtons) do
        if name == tabName then
            btn.BackgroundColor3 = Color3.fromRGB(0, 90, 210)
            btn.TextColor3       = Color3.fromRGB(255, 255, 255)
        else
            btn.BackgroundColor3 = Color3.fromRGB(14, 16, 28)
            btn.TextColor3       = Color3.fromRGB(150, 150, 170)
        end
    end
end

local function AddTabButton(name, label)
    local btn = Instance.new("TextButton", Sidebar)
    btn.Size             = UDim2.new(0, 112, 0, 36)
    btn.Text             = label
    btn.Font             = Enum.Font.GothamBold
    btn.TextSize         = 11
    btn.TextXAlignment   = Enum.TextXAlignment.Left
    btn.TextColor3       = Color3.fromRGB(150, 150, 170)
    btn.BackgroundColor3 = Color3.fromRGB(14, 16, 28)
    btn.ZIndex           = 4
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
    local s = Instance.new("UIStroke", btn)
    s.Color     = Color3.fromRGB(25, 28, 48)
    s.Thickness = 1
    local pad = Instance.new("UIPadding", btn)
    pad.PaddingLeft = UDim.new(0, 10)
    btn.MouseButton1Click:Connect(function() SwitchTab(name) end)
    TabButtons[name] = btn
end

AddTabButton("ESP",     "  ESP")
AddTabButton("SDAMAGE", "  SDAMAGE")
AddTabButton("SPY",     "  REMOTE SPY")

local function AddToggle(page, name, target, key)
    local Frame = Instance.new("Frame", page)
    Frame.Size                   = UDim2.new(1, -6, 0, 40)
    Frame.BackgroundColor3       = Color3.fromRGB(10, 11, 20)
    Frame.BackgroundTransparency = 0.05
    Frame.ZIndex                 = 4
    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 8)
    local FS = Instance.new("UIStroke", Frame)
    FS.Color     = Color3.fromRGB(22, 26, 44)
    FS.Thickness = 1

    local TL = Instance.new("TextLabel", Frame)
    TL.Text              = name
    TL.Size              = UDim2.new(0.68, 0, 1, 0)
    TL.Position          = UDim2.new(0, 12, 0, 0)
    TL.Font              = Enum.Font.GothamBold
    TL.TextSize          = 11
    TL.TextColor3        = Color3.fromRGB(220, 220, 235)
    TL.BackgroundTransparency = 1
    TL.TextXAlignment    = Enum.TextXAlignment.Left
    TL.ZIndex            = 5

    local Switch = Instance.new("TextButton", Frame)
    Switch.Text             = ""
    Switch.Size             = UDim2.new(0, 42, 0, 20)
    Switch.Position         = UDim2.new(1, -50, 0.5, -10)
    Switch.BackgroundColor3 = Color3.fromRGB(22, 26, 44)
    Switch.ZIndex           = 5
    Instance.new("UICorner", Switch).CornerRadius = UDim.new(0, 10)

    local Circle = Instance.new("Frame", Switch)
    Circle.Size             = UDim2.new(0, 14, 0, 14)
    Circle.Position         = UDim2.new(0, 3, 0, 3)
    Circle.BackgroundColor3 = Color3.fromRGB(120, 120, 140)
    Circle.ZIndex           = 6
    Instance.new("UICorner", Circle).CornerRadius = UDim.new(0, 7)

    local function Upd()
        if target[key] then
            Switch.BackgroundColor3 = Color3.fromRGB(0, 60, 170)
            Circle.BackgroundColor3 = Color3.fromRGB(0, 200, 255)
            Circle.Position         = UDim2.new(0, 25, 0, 3)
            FS.Color                = Color3.fromRGB(0, 170, 255)
        else
            Switch.BackgroundColor3 = Color3.fromRGB(22, 26, 44)
            Circle.BackgroundColor3 = Color3.fromRGB(120, 120, 140)
            Circle.Position         = UDim2.new(0, 3, 0, 3)
            FS.Color                = Color3.fromRGB(22, 26, 44)
        end
    end
    Switch.MouseButton1Click:Connect(function() target[key] = not target[key]; Upd() end)
    Upd()
end

local function AddSlider(page, label, minVal, maxVal, targetTable, key)
    local Bg = Instance.new("Frame", page)
    Bg.Size                   = UDim2.new(1, -6, 0, 50)
    Bg.BackgroundColor3       = Color3.fromRGB(10, 11, 20)
    Bg.BackgroundTransparency = 0.05
    Bg.ZIndex                 = 4
    Instance.new("UICorner", Bg).CornerRadius = UDim.new(0, 8)
    local BS = Instance.new("UIStroke", Bg)
    BS.Color     = Color3.fromRGB(22, 26, 44)
    BS.Thickness = 1

    local LT = Instance.new("TextLabel", Bg)
    LT.Text             = label .. ":  " .. targetTable[key]
    LT.Size             = UDim2.new(1, 0, 0, 20)
    LT.Position         = UDim2.new(0, 12, 0, 4)
    LT.Font             = Enum.Font.GothamMedium
    LT.TextColor3       = Color3.fromRGB(0, 200, 255)
    LT.TextSize         = 11
    LT.BackgroundTransparency = 1
    LT.TextXAlignment   = Enum.TextXAlignment.Left
    LT.ZIndex           = 5

    local Track = Instance.new("Frame", Bg)
    Track.Size             = UDim2.new(1, -24, 0, 3)
    Track.Position         = UDim2.new(0, 12, 0, 34)
    Track.BackgroundColor3 = Color3.fromRGB(22, 26, 44)
    Track.ZIndex           = 5
    Instance.new("UICorner", Track).CornerRadius = UDim.new(0, 2)

    local Fill = Instance.new("Frame", Track)
    Fill.Size             = UDim2.new((targetTable[key]-minVal)/(maxVal-minVal), 0, 1, 0)
    Fill.BackgroundColor3 = Color3.fromRGB(0, 180, 255)
    Fill.ZIndex           = 6
    Instance.new("UICorner", Fill).CornerRadius = UDim.new(0, 2)

    local Knob = Instance.new("Frame", Track)
    Knob.Size             = UDim2.new(0, 10, 0, 10)
    Knob.AnchorPoint      = Vector2.new(0.5, 0.5)
    Knob.Position         = UDim2.new((targetTable[key]-minVal)/(maxVal-minVal), 0, 0.5, 0)
    Knob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    Knob.ZIndex           = 7
    Instance.new("UICorner", Knob).CornerRadius = UDim.new(0, 5)

    local function Set(ip)
        local pct = math.clamp((ip.X - Track.AbsolutePosition.X) / Track.AbsoluteSize.X, 0, 1)
        local val = minVal + pct * (maxVal - minVal)
        val = maxVal <= 10 and (math.floor(val*10)/10) or math.floor(val)
        targetTable[key]      = val
        LT.Text               = label .. ":  " .. val
        Fill.Size             = UDim2.new(pct, 0, 1, 0)
        Knob.Position         = UDim2.new(pct, 0, 0.5, 0)
        Fill.BackgroundColor3 = currentAutoColor
    end
    local drag = false
    Bg.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            drag = true; Set(i.Position)
        end
    end)
    Bg.InputEnded:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            drag = false
        end
    end)
    UserInputService.InputChanged:Connect(function(i)
        if drag and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
            Set(i.Position)
        end
    end)
end

local function AddSelector(page, name, options, targetTable, key)
    local Frame = Instance.new("Frame", page)
    Frame.Size                   = UDim2.new(1, -6, 0, 40)
    Frame.BackgroundColor3       = Color3.fromRGB(10, 11, 20)
    Frame.BackgroundTransparency = 0.05
    Frame.ZIndex                 = 4
    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 8)
    local FS = Instance.new("UIStroke", Frame)
    FS.Color     = Color3.fromRGB(22, 26, 44)
    FS.Thickness = 1

    local TL = Instance.new("TextLabel", Frame)
    TL.Text              = name
    TL.Size              = UDim2.new(0.5, 0, 1, 0)
    TL.Position          = UDim2.new(0, 12, 0, 0)
    TL.Font              = Enum.Font.GothamBold
    TL.TextSize          = 11
    TL.TextColor3        = Color3.fromRGB(220, 220, 235)
    TL.BackgroundTransparency = 1
    TL.TextXAlignment    = Enum.TextXAlignment.Left
    TL.ZIndex            = 5

    local Btn = Instance.new("TextButton", Frame)
    Btn.Font             = Enum.Font.GothamBold
    Btn.TextSize         = 10
    Btn.TextColor3       = Color3.fromRGB(0, 200, 255)
    Btn.BackgroundColor3 = Color3.fromRGB(16, 19, 34)
    Btn.Size             = UDim2.new(0, 96, 0, 24)
    Btn.Position         = UDim2.new(1, -102, 0.5, -12)
    Btn.ZIndex           = 5
    Instance.new("UICorner", Btn).CornerRadius = UDim.new(0, 6)
    local BS = Instance.new("UIStroke", Btn)
    BS.Color     = Color3.fromRGB(35, 40, 65)
    BS.Thickness = 1

    local idx = 1
    for i, v in ipairs(options) do if targetTable[key] == v then idx = i; break end end
    local function Upd() targetTable[key] = options[idx]; Btn.Text = "< " .. options[idx] .. " >" end
    Btn.MouseButton1Click:Connect(function() idx = (idx % #options) + 1; Upd() end)
    Upd()
end

AddToggle(EspPage,   "MASTER ESP",   Settings.ESP, "Enabled")
AddToggle(EspPage,   "BOX",          Settings.ESP, "Box")
AddToggle(EspPage,   "LINE",         Settings.ESP, "Line")
AddSelector(EspPage, "LINE POS",     {"TOP", "CENTER", "BOTTOM"}, Settings.ESP, "LinePos")
AddToggle(EspPage,   "SKELETON",     Settings.ESP, "Skeleton")
AddToggle(EspPage,   "NAME TAG",     Settings.ESP, "Name")
AddToggle(EspPage,   "HEALTH BAR",   Settings.ESP, "Health")
AddSlider(EspPage,   "THICKNESS",    0.5, 3, Settings.ESP, "Thickness")

AddToggle(SdamagePage, "ENABLE SDAMAGE", Settings.Sdamage, "Enabled")
AddSlider(SdamagePage, "DAMAGE",         1, 999, Settings.Sdamage, "Amount")

local SpyClearRow = Instance.new("Frame", SpyPage)
SpyClearRow.Size             = UDim2.new(1, -6, 0, 34)
SpyClearRow.BackgroundColor3 = Color3.fromRGB(10, 11, 20)
SpyClearRow.BackgroundTransparency = 0.05
SpyClearRow.ZIndex           = 4
Instance.new("UICorner", SpyClearRow).CornerRadius = UDim.new(0, 8)
Instance.new("UIStroke", SpyClearRow).Color        = Color3.fromRGB(22, 26, 44)

local SpyCountLabel = Instance.new("TextLabel", SpyClearRow)
SpyCountLabel.Text              = "REMOTE SPY — 0 entries"
SpyCountLabel.Size              = UDim2.new(0.6, 0, 1, 0)
SpyCountLabel.Position          = UDim2.new(0, 10, 0, 0)
SpyCountLabel.Font              = Enum.Font.GothamBold
SpyCountLabel.TextSize          = 11
SpyCountLabel.TextColor3        = Color3.fromRGB(0, 200, 255)
SpyCountLabel.BackgroundTransparency = 1
SpyCountLabel.TextXAlignment    = Enum.TextXAlignment.Left
SpyCountLabel.ZIndex            = 5

local SpyClearBtn = Instance.new("TextButton", SpyClearRow)
SpyClearBtn.Text             = "CLEAR"
SpyClearBtn.Size             = UDim2.new(0, 60, 0, 22)
SpyClearBtn.Position         = UDim2.new(1, -68, 0.5, -11)
SpyClearBtn.Font             = Enum.Font.GothamBold
SpyClearBtn.TextSize         = 10
SpyClearBtn.TextColor3       = Color3.fromRGB(255, 80, 80)
SpyClearBtn.BackgroundColor3 = Color3.fromRGB(40, 10, 10)
SpyClearBtn.ZIndex           = 5
Instance.new("UICorner", SpyClearBtn).CornerRadius = UDim.new(0, 6)

local spyEntryCount = 0

SpyAddEntry = function(method, fullPath, argStr, copyText)
    spyEntryCount = spyEntryCount + 1
    SpyCountLabel.Text = "REMOTE SPY — " .. spyEntryCount .. " entries"

    local row = Instance.new("Frame", SpyPage)
    row.Size             = UDim2.new(1, -6, 0, 52)
    row.BackgroundColor3 = Color3.fromRGB(8, 10, 18)
    row.BackgroundTransparency = 0.05
    row.ZIndex           = 4
    Instance.new("UICorner", row).CornerRadius = UDim.new(0, 7)
    local rs = Instance.new("UIStroke", row)
    rs.Color     = Color3.fromRGB(0, 80, 180)
    rs.Thickness = 0.8

    local methodTag = Instance.new("TextLabel", row)
    methodTag.Text       = "[" .. method .. "]"
    methodTag.Size       = UDim2.new(0, 120, 0, 18)
    methodTag.Position   = UDim2.new(0, 8, 0, 4)
    methodTag.Font       = Enum.Font.GothamBold
    methodTag.TextSize   = 10
    methodTag.TextColor3 = method == "FireServer" and Color3.fromRGB(255, 180, 0) or Color3.fromRGB(180, 100, 255)
    methodTag.BackgroundTransparency = 1
    methodTag.TextXAlignment = Enum.TextXAlignment.Left
    methodTag.ZIndex     = 5

    local pathLabel = Instance.new("TextLabel", row)
    pathLabel.Text       = fullPath
    pathLabel.Size       = UDim2.new(1, -70, 0, 16)
    pathLabel.Position   = UDim2.new(0, 8, 0, 20)
    pathLabel.Font       = Enum.Font.Gotham
    pathLabel.TextSize   = 10
    pathLabel.TextColor3 = Color3.fromRGB(200, 200, 220)
    pathLabel.BackgroundTransparency = 1
    pathLabel.TextXAlignment = Enum.TextXAlignment.Left
    pathLabel.TextTruncate   = Enum.TextTruncate.AtEnd
    pathLabel.ZIndex     = 5

    local argLabel = Instance.new("TextLabel", row)
    argLabel.Text       = argStr == "" and "(no args)" or argStr
    argLabel.Size       = UDim2.new(1, -70, 0, 14)
    argLabel.Position   = UDim2.new(0, 8, 0, 36)
    argLabel.Font       = Enum.Font.Gotham
    argLabel.TextSize   = 9
    argLabel.TextColor3 = Color3.fromRGB(120, 150, 200)
    argLabel.BackgroundTransparency = 1
    argLabel.TextXAlignment = Enum.TextXAlignment.Left
    argLabel.TextTruncate   = Enum.TextTruncate.AtEnd
    argLabel.ZIndex     = 5

    local copyBtn = Instance.new("TextButton", row)
    copyBtn.Text             = "COPY"
    copyBtn.Size             = UDim2.new(0, 48, 0, 20)
    copyBtn.Position         = UDim2.new(1, -54, 0.5, -10)
    copyBtn.Font             = Enum.Font.GothamBold
    copyBtn.TextSize         = 9
    copyBtn.TextColor3       = Color3.fromRGB(0, 200, 255)
    copyBtn.BackgroundColor3 = Color3.fromRGB(0, 30, 60)
    copyBtn.ZIndex           = 5
    Instance.new("UICorner", copyBtn).CornerRadius = UDim.new(0, 5)

    copyBtn.MouseButton1Click:Connect(function()
        setclipboard(copyText)
        copyBtn.Text      = "OK!"
        copyBtn.TextColor3 = Color3.fromRGB(0, 255, 100)
        task.delay(1.2, function()
            copyBtn.Text      = "COPY"
            copyBtn.TextColor3 = Color3.fromRGB(0, 200, 255)
        end)
    end)

    row.Parent = SpyPage
end

SpyClearBtn.MouseButton1Click:Connect(function()
    for _, child in pairs(SpyPage:GetChildren()) do
        if child:IsA("Frame") and child ~= SpyClearRow then
            child:Destroy()
        end
    end
    spyEntryCount      = 0
    SpyCountLabel.Text = "REMOTE SPY — 0 entries"
end)

RunService.RenderStepped:Connect(function()
    MainStroke.Color     = currentAutoColor
    ToggleStroke.Color   = currentAutoColor
    ToggleBtn.TextColor3 = currentAutoColor
end)

SwitchTab("ESP")
